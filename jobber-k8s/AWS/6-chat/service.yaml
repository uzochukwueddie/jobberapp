apiVersion: v1
kind: Service
metadata:
  name: jobber-chat
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: jobber-chat
  ports:
  - name: chat
    port: 4005
    targetPort: 4005
    protocol: TCP
